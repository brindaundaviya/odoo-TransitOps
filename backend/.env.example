# Server
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/transitops?schema=public"

# JWT (configuration only — auth logic not yet implemented)
JWT_SECRET=your-super-secret-jwt-key-min-32-characters
JWT_EXPIRES_IN=1h

# Bcrypt (installed — auth logic not yet implemented)
BCRYPT_SALT_ROUNDS=10

# CORS
CORS_ORIGIN=http://localhost:5173

# Seed admin (after running prisma:seed)
# Email: admin@transitops.demo
# Password: Admin@12345
